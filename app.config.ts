// app.config.ts 全域共用的資料
export default defineAppConfig({
    theme: {
      color: '#0d6efd'
    }
  })


  // const appConfig = useAppConfig(); 在頁面使用
  // console.log(appConfig.theme.color); // output: #0d6efd